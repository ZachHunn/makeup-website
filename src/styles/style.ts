import { createTheme } from '@nextui-org/react';

export const theme = createTheme({
  theme: {
    colors: {
      primary: '$FAD2FC',
      lavenderBlue: '$CCCCFC',
      lavenderWeb: '$E0ECFF',
      ivory: '$FFFEED',
      blanchedAlmond: '$FFEBC9',
      bubbleGum: '$FFC4CD',
    },
  },
});
