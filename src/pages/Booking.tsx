import { Text } from '@nextui-org/react';
import { NextPage } from 'next';

const Booking: NextPage = (): JSX.Element => {
  return (
    <>
      <Text h1 css={{ paddingLeft: '25px' }} size={60}></Text>
    </>
  );
};

export default Booking;
